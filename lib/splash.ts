// Shared with AutoRedirect so a returning user's redirect waits for the
// splash to actually be seen instead of firing before it finishes.
export const SPLASH_VISIBLE_MS = 1500;
export const SPLASH_FADE_MS = 300;
